// Load all our javascript
require('./scripts/index');

// Load our base stylesheets. This stylesheet should be fairly minimal, as
// we'll be requiring specific component styles in the JS file where the
// component is defined.
require('./stylesheets/base');
